%li.list
  = link_to("/champions/#{@champion.key}/#{skin.name}") do
    .list__meta
      %p.list__meta__name
        - if skin.name == "default"
          = @champion.name
        - else
          = skin.name
    .list__image
      %img{ alt: skin.name , src: "https://ddragon.leagueoflegends.com/cdn/img/champion/loading/#{@champion.key}_#{skin.skin_num}.jpg" }/
